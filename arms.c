#include <stdio.h>
int main()
{
    int t,a,b,c;
    printf("enter the value of t,a,b,c");
    scanf("%d %d %d %d",&t,&a,&b,&c);
    if
    {
        ((a*a*a)+(b*b*b)+(c*c*c))=t
        printf("it is armstrong number");
        else
        {
            ((a*a*a)+(b*b*b)+(c*c*c))=!0
            printf("it is not a armstrong number");
        }
    }
}
